<common:LayoutAwarePage
    x:Name="pageRoot"
    x:Class="Music8.Pages.NowPlaying"
    DataContext="{Binding DefaultViewModel, RelativeSource={RelativeSource Self}}"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Music8.Pages"
    xmlns:common="using:Music8.Common"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid>
        <Image x:Name="artistBackground" VerticalAlignment="Stretch" HorizontalAlignment="Stretch" Stretch="UniformToFill" Opacity=".2"/>
        <!--<Rectangle Width="360" VerticalAlignment="Stretch" HorizontalAlignment="Right">
            <Rectangle.Fill>
                <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                    <GradientStop Offset="0" Color="#00FFFFFF"/>
                    <GradientStop Offset="1" Color="#55000000"/>
                </LinearGradientBrush>
            </Rectangle.Fill>
        </Rectangle>-->
        <ListView x:ConnectionId='1' x:Name="nowPlayingList"
                  Width="360" HorizontalAlignment="Right"
                  Margin="0,75,0,100" IsItemClickEnabled="False"
                                                                      
                  AllowDrop="True"
                  SelectionMode="Single"
                  CanReorderItems="True"
                                              >
            <ListView.ItemTemplate>
                <DataTemplate>
                    <Grid Width="300" HorizontalAlignment="Stretch" Margin="6">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <StackPanel Margin="5" DataContext="{Binding song}">
                            <TextBlock Text="{Binding Title}" TextWrapping="NoWrap"/>
                            <TextBlock Text="{Binding AlbumArtist}" FontSize="12" TextWrapping="NoWrap"/>
                            <TextBlock TextWrapping="NoWrap">
                                    <Run Text="{Binding DurationTimeSpan}"/>
                            </TextBlock>
                        </StackPanel>
                        <Button x:ConnectionId='2' Content="&#xE10A;" Grid.Column="1"
                                Style="{StaticResource CustomButtonStyle}"
                                                    HorizontalAlignment="Right"
                                Tag="{Binding}"/>
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</common:LayoutAwarePage>

